mutation BookmarkFlyer($uuid: String!) {
  bookmarkArticle(uuid: $uuid) {
    id
  }
}

mutation IncrementTimesClicked($id: String!, $uuid: String!) {
  incrementTimesClicked(id:$id, uuid:$uuid) {
    id
  }
}
